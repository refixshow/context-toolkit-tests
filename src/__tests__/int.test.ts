describe("int testing", () => {
  test("should work", () => {
    expect(true).toBeTruthy();
  });
});

export {};

// actions czy działają wśród komponentów
// store czy zmienia się odnośnie do akcji
// provider czy przekazuje do innych store do innych komponentów
// onload czy robi to co powinien

// PROVIDER
// name, displayname
